assets::
	rm -f */*~
	go run github.com/shuLhan/go-bindata/cmd/go-bindata -o assets.go assets/*
	
run: assets
	go run . . 

test: assets
	go install ./...
	cd /Users/jfesler/go-oanda-epub/public && static-indexes -r . && open index.html
	
